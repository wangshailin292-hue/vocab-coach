# Use :80 for IP-only HTTP access, or a real domain like vocab.example.com for HTTPS.
SITE_ADDRESS=:80

# Optional server-side API keys. You can also leave these empty and enter keys in the web settings page.
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
